# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.packages
build/

example_console/image/main.exe
linux/libwarp_derive.so
linux/libwarp_fs_ipfs.so
linux/libwarp_fs_memory.so
linux/libwarp_fs_storj.so
linux/libwarp_mp_ipfs.so
linux/libwarp_mp_solana.so
linux/libwarp_pd_flatfile.so
linux/libwarp_pd_memory.so
linux/libwarp_pd_stretto.so
linux/libwarp_rg_ipfs.so
linux/libwarp.so
